apiVersion: v1
kind: Service
metadata:
  name: drive-service
  namespace: bits-scalable-services
spec:
  type: NodePort
  selector:
    app: drive-service
  ports:
    - port: 9582
      targetPort: 9582
